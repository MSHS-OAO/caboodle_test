---
title: "Microsoft SQl Server Test"
output: html_document
---

```{r setup}
library(DBI)
library(odbc)

# con <- dbConnect(odbc(), Driver = "SQLServer",
#                  Server = "SEPIBOJP016001",
#                  Port = 1433,
#                  #Database = "qidm_dev.dbo",
#                  # Authentication = "ActiveDirectoryIntegrated"
#                  Trusted_Connection = TRUE,
#                  Integrated_Security = TRUE#,
#                  #uid = "MSSMCAMPUS\villea04",
#                  #pwd = "Ingermanycovid2024"#,
#                  #Server_SPN = "MSSMCAMPUS"
# )

 con <- dbConnect(odbc(),
                  Driver = "SQLServer",
                  Server = "SQOPSQLP015001",
#                  #Database = "PRDREPORT.FullAccess.Referrals",
                  UID = "kweons01",
                  PWD = "Pasword01#PasPasword01#Pas",
                  Port = 1433)

#con <- dbConnect(odbc(), "HandHygiene")

 print(con)

#referrals_raw <- dbGetQuery(con, "SELECT TOP 10 * FROM PRDREPORT.FullAccess.Referrals") 

#print(referrals_raw)







```

